import type { NextConfig } from "next";

// Iteração 2: exemplo de alteração mínima (exercício de branches e merge).
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
